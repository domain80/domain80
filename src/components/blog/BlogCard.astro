---
interface Props {
  title: string;
  description: string;
  slug: string;
}

const { title, description, slug } = Astro.props;
---

<article class="group">
  <a
    href={`/blog/${slug}`}
    class="block p-4 rounded-lg border border-gray-200/90 dark:border-gray-700/60 backdrop-blur-sm bg-white/50 dark:bg-gray-900/50 hover:border-accent/50 dark:hover:border-accent/50 transition-all duration-200"
  >
    <div class="flex items-start gap-3">
      <!-- Red dot indicator -->
      <div class="w-2 h-2 rounded-full bg-red-500 mt-2 flex-shrink-0"></div>

      <div class="space-y-2 flex-1">
        <h3
          class="text-lg font-semibold text-gray-900 dark:text-gray-100 group-hover:text-accent dark:group-hover:text-accent transition-colors"
        >
          {title}
        </h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 line-clamp-2">
          {description}
        </p>
      </div>
    </div>
  </a>
</article>
